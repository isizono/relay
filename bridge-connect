#!/usr/bin/env python3
"""bridge-connect — SSH forced command ラッパー（エントリーポイント）。

ロジックは bridge_connect.py に集約。このファイルは実行可能スクリプトとして
PATH に置かれ、authorized_keys の forced command から呼ばれる。
"""
import sys
from pathlib import Path

# 同ディレクトリの bridge_connect.py をインポート
sys.path.insert(0, str(Path(__file__).parent))
import bridge_connect as bc

if __name__ == "__main__":
    bc.main()
